{
    "name": "isense-tests",
    "version": "1.0.0",
    "description": "Test suite for the More Informative Untyped IntelliSense project",
    "scripts": {
        "test": "jest",
        "test:watch": "jest --watch",
        "test:coverage": "jest --coverage",
        "test:unit": "jest --testPathPattern=unit",
        "test:integration": "jest --testPathPattern=integration"
    },
    "devDependencies": {
        "@types/jest": "^29.5.5",
        "jest": "^29.7.0",
        "typescript": "^5.9.3"
    },
    "dependencies": {
        "vscode-languageserver": "^9.0.1"
    }
}
